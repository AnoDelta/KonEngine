linux:
	glfw
	GL
	dl
	pthread
	X11, Xrandr, Xi

windows:
	glfw3 (already bundled)
	opengl32 (should be installed by default)
	gdi32 (should be installed by default)
	winmm (should be installed by default)

